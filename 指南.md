
    1.打开vscode,打开内置终端,直接输入docker compose up -d (只需要一次，本命令是在docker中安装CrarveNex，如果已经有了，无需操作)
    2.打开docker软件，查看是否CarveNex是否全部启动
    3.打开文件夹，找到代码文件夹CarveNex\web\react，输入cmd
    4.在这个终端输入 npm install（如果web\react文件夹下面已经存在node_moudel）,无需操作这步
    5.无论（4）是否操作，在此终端输入npm run dev，讲服务启动
    6.首页为localhost:12700，管理后台为localhost:12700/admin
    默认管理员账号：fuyuxi 密码：fyx060915

    如果想新创管理员
    进入到vscode,打开内置终端，（前提保证docker在运行），输入docker compose exec web bash，（看到root@xxx开头说明进去了），再输入python manage.py createsuperuser，根据提示创建即可
    清 Docker 构建缓存：docker builder prune -af